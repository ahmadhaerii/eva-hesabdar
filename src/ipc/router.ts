import { app } from "./app";
import { product } from "./products";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";

export const router = {
  app,
  shell,
  theme,
  window,
  product,
};
